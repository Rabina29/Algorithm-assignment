#include<bits/stdc++.h>
using namespace std;
int countdigit(long long int n)
{
 if(n==0) return 1;
 if(n<10) return 1;
 else return 1+countdigit(n/10);
}
int main()
{
 long long int x;
 cin >> x;
 cout << countdigit(x) << endl;
 return 0;
}

